/**
 * Moving backgrounds are intentionally disabled. Room imagery is supplied by
 * the authoritative room payload, whose Storage paths are served from Blob.
 */
export function videoForScene(_slug: string): string | undefined {
  return undefined;
}
